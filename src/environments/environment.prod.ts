// src/environments/environment.prod.ts
// Used during: ng build (production)

export const environment = {
  production: true,
  apiUrl: 'https://your-production-api.com/api'  // replace with your deployed backend URL
};
